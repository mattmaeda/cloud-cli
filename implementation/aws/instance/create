#!/usr/bin/python

ARGS = [
    {'name': 'access', 'required': True, 'help': 'AWS access key'},
    {'name': 'secret', 'required': True, 'help': 'AWS secret key'}
]


def execute(args):
    print "AWS CREATE"
